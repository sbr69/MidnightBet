{
  "name": "midnightbet",
  "private": true,
  "scripts": {
    "compile": "npm run compile --prefix contract",
    "copy-zk": "node scripts/copy-zk-artifacts.mjs",
    "proof-server": "docker compose up -d",
    "frontend": "node scripts/copy-zk-artifacts.mjs && npm run dev --prefix frontend",
    "test": "npm run test --prefix dapp && npm run test --prefix contract",
    "prepare-deploy": "npm run prepare-deploy --prefix dapp"
  },
  "devDependencies": {
    "@types/node": "^26.1.0"
  }
}
